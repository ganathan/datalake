AWSTemplateFormatVersion: 2010-09-09
Description: Raw Metadata Purger Lambda for data lake as a service - Core

Parameters:
  Entity:
    Type: String
    Default: ganesh
  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, tst, int, uat, prd, poc]
  LambdaZipFileName:
    Type: String

Resources:
  CreateDaaSRawMetadataPurgeLambda:
    Type: 'AWS::CloudFormation::Stack'
    Properties :
      Parameters:
        Description: !Sub Datalake as a Service Core raw metadata purger deployed in ${Environment}
        Entity: !Ref Entity
        Environment: !Ref Environment
        HandlerName: lmd-raw-metadata-purger.lambda_handler        
        LambdaCompressedFileName: !Sub ${LambdaZipFileName}
        MemorySize: 128
        Name: !Sub ${Entity}-lmd-raw-metadata-purger-${Environment}        
        Timeout: 120
      TemplateURL: !Sub https://s3-${AWS::Region}.amazonaws.com/${Entity}-s3-${AWS::AccountId}-${AWS::Region}-common-artifacts-${Environment}/lmd/scripts/template/cft-lmd-common-template.yml
      TimeoutInMinutes : '60'

  CreateDaaSRawMetadataPurgePolicy:
    Type: 'AWS::IAM::Policy'
    Properties:
      Roles:
        - !GetAtt CreateDaaSRawMetadataPurgeLambda.Outputs.LambdaRole
      PolicyName: plc-inline-lmd-raw-metadata-pruge
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          -
            Sid: "assumeDaaSGlueControllerClientRole"
            Effect: "Allow"
            Action: "sts:AssumeRole"
            Resource: 
              Fn::Sub: "arn:aws:iam::*:role/${Entity}-rle-glue-controller-admin-${Environment}"
          -
            Sid: "assumeDaaSMetadataPurgeRole"
            Effect: "Allow"
            Action: "sts:AssumeRole"
            Resource: 
              Fn::Sub: "arn:aws:iam::*:role/rle-${Entity}-lmd-metadata-purger-${Environment}"
